# Backend API URLs
# Development URL (local backend server)
VITE_BACKEND_URL=http://localhost:3000

# Production URL (deployed backend)
VITE_BACKEND_URL_PROD=https://ghss-management-backend.vercel.app

# Note: Copy this file to .env and update the values according to your environment
# The application will automatically use VITE_BACKEND_URL in development mode
# and VITE_BACKEND_URL_PROD in production mode
